CREATE TABLE codelens (
    type varchar NOT NULL,
    source_file varchar NOT NULL,
    data other NOT NULL
);

CREATE TABLE diagnostic (
    type varchar NOT NULL,
    source_file varchar NOT NULL,
    data other NOT NULL
);
